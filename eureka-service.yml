server:
  port: 8761

spring:
  application:
    name: eureka-service

# Eureka server: do not register with itself
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: localhost
